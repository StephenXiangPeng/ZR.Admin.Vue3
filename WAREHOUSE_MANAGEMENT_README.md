# 仓库管理系统

## 功能概述

这个页面整合了三个管理模块，每个模块都支持独立的折叠/展开功能：

### 1. 仓库管理
- **功能**：管理仓库的基本信息，包括仓库编号、名称、类型、状态等
- **特性**：
  - 支持搜索和筛选（编号/名称、状态、仓库类型、库位管理状态）
  - 支持新增、编辑、删除、启用/禁用操作
  - 支持导入功能
  - 分页显示
- **数据字段**：编号、名称、状态、仓库类型、启用库位管理、联系人、联系电话、创建人、创建时间、最后更新人、最后更新时间

### 2. 库区管理
- **功能**：管理仓库内的库区信息
- **特性**：
  - 支持搜索和筛选（库区名称/编号、仓库名称、状态）
  - 支持新增、编辑、删除、启用/禁用操作
  - 支持导入功能
  - 分页显示
- **数据字段**：编号、库区名称、所属仓库、所属类型、备注、使用状态、创建人、创建时间、最后修改人、最后修改时间

### 3. 库位管理
- **功能**：管理库区内的具体库位信息
- **特性**：
  - 支持搜索和筛选（库位编号/名称、仓库名称、状态）
  - 支持新增、编辑、删除、启用/禁用操作
  - 支持导入功能
  - 分页显示
- **数据字段**：编号、库位名称、所属库区、所属仓库、所属类型、备注、使用状态、创建人、创建时间、最后修改人、最后修改时间

## 技术特性

### 折叠功能
- 每个管理模块都有独立的折叠/展开控制
- 使用 `v-show` 指令控制内容显示
- 折叠状态通过 `ref` 变量管理
- 支持动画过渡效果

### 响应式设计
- 支持不同屏幕尺寸的适配
- 移动端友好的布局调整
- 表单控件宽度自适应

### 数据管理
- 每个模块都有独立的数据状态管理
- 支持分页查询
- 模拟数据展示（可替换为实际API调用）

## 使用方法

### 基本操作
1. **查看数据**：页面加载时自动获取所有模块的数据
2. **搜索筛选**：使用各模块顶部的搜索表单进行数据筛选
3. **折叠/展开**：点击各模块标题右侧的"收起"/"展开"按钮
4. **数据操作**：使用表格中的操作按钮进行增删改查操作

### 新增操作
1. 点击对应模块的"新增"按钮
2. 在弹出的对话框中填写相关信息
3. 点击"确定"保存

### 编辑操作
1. 点击表格中对应行的"编辑"按钮
2. 在弹出的对话框中修改信息
3. 点击"确定"保存修改

### 删除操作
1. 点击表格中对应行的"删除"按钮
2. 确认删除操作
3. 数据将被删除（当前为模拟操作）

## 文件结构

```
src/views/basicdata/warehousemanagement.vue
├── 模板部分 (Template)
│   ├── 仓库管理区域
│   ├── 库区管理区域
│   └── 库位管理区域
├── 脚本部分 (Script)
│   ├── 状态管理
│   ├── 数据查询函数
│   ├── 操作处理函数
│   └── 生命周期钩子
└── 样式部分 (Style)
    ├── 基础样式
    ├── 折叠动画
    └── 响应式设计
```

## 注意事项

1. **API集成**：当前使用模拟数据，需要替换为实际的API调用
2. **权限控制**：可以根据需要添加权限验证
3. **数据验证**：表单验证规则已配置，可根据业务需求调整
4. **错误处理**：建议添加更完善的错误处理机制

## 扩展建议

1. **批量操作**：可以添加批量删除、批量导入等功能
2. **数据导出**：可以添加Excel导出功能
3. **高级搜索**：可以添加更多搜索条件和组合查询
4. **数据统计**：可以添加数据统计和图表展示
5. **操作日志**：可以添加操作历史记录功能
