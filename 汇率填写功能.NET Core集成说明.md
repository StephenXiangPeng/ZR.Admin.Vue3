# 汇率填写功能 .NET Core 集成说明

## 概述
本文档说明了如何在 .NET Core 项目中集成汇率填写功能，包括实体模型、服务层、控制器和依赖注入配置。

## 文件结构

```
ZR.Admin.Vue3/
├── Models/
│   └── ExchangeRate/
│       ├── ExchangeRateTaskConfig.cs          # 汇率任务配置实体
│       ├── ExchangeRateRecord.cs              # 汇率填写记录实体
│       ├── ExchangeRateNotificationLog.cs     # 通知日志实体
│       ├── ExchangeRateHistory.cs             # 汇率历史记录实体
│       └── ViewModels/                        # 视图模型
│           ├── ExchangeRateStatsViewModel.cs
│           ├── UserExchangeRateStatusViewModel.cs
│           └── RequestModels.cs
├── Services/
│   └── ExchangeRate/
│       ├── IExchangeRateService.cs            # 服务接口
│       └── ExchangeRateService.cs             # 服务实现
├── Controllers/
│   └── ExchangeRate/
│       ├── ExchangeRateTaskController.cs      # 主要API控制器
│       └── ExchangeRateManageController.cs    # 管理功能控制器
└── Extensions/
    └── ServiceCollectionExtensions.cs         # 依赖注入配置
```

## 集成步骤

### 1. 安装必要的 NuGet 包

```xml
<PackageReference Include="SqlSugar" Version="5.1.4.156" />
<PackageReference Include="Microsoft.AspNetCore.Mvc.NewtonsoftJson" Version="6.0.0" />
```

### 2. 配置 SqlSugar

在 `appsettings.json` 中配置数据库连接：

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=YourDatabase;Uid=root;Pwd=password;"
  }
}
```

在 `Startup.cs` 或 `Program.cs` 中配置 SqlSugar：

```csharp
// 在 ConfigureServices 方法中
services.AddSingleton<ISqlSugarClient>(provider =>
{
    var config = new ConnectionConfig
    {
        ConnectionString = Configuration.GetConnectionString("DefaultConnection"),
        DbType = DbType.MySql,
        IsAutoCloseConnection = true,
        InitKeyType = InitKeyType.Attribute
    };
    
    var db = new SqlSugarClient(config);
    
    // 配置日志
    db.Aop.OnLogExecuting = (sql, pars) =>
    {
        Console.WriteLine($"SQL: {sql}");
    };
    
    return db;
});
```

### 3. 注册服务

在 `Startup.cs` 或 `Program.cs` 中注册汇率填写服务：

```csharp
// 在 ConfigureServices 方法中
services.AddExchangeRateServices();
```

### 4. 配置认证（如果需要）

如果使用 JWT 认证，确保在 `GetCurrentUserId()` 方法中正确获取用户ID：

```csharp
private int GetCurrentUserId()
{
    var userIdClaim = User.FindFirst("user_id");
    if (userIdClaim != null && int.TryParse(userIdClaim.Value, out int userId))
    {
        return userId;
    }
    return 0;
}
```

### 5. 数据库迁移

执行建表语句创建数据库表结构：

```sql
-- 执行 汇率填写功能MySQL建表语句.sql 文件中的SQL语句
```

## API 接口说明

### 主要接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取配置 | GET | `/api/ExchangeRateTask/GetConfig` | 获取汇率更新任务配置 |
| 保存配置 | POST | `/api/ExchangeRateTask/SaveConfig` | 保存汇率更新任务配置 |
| 检查用户需求 | GET | `/api/ExchangeRateTask/CheckUserNeedFill` | 检查用户是否需要填写汇率 |
| 提交汇率 | POST | `/api/ExchangeRateTask/SubmitExchangeRate` | 提交汇率信息 |
| 获取记录 | GET | `/api/ExchangeRateTask/GetRecords` | 获取汇率填写记录 |
| 获取统计 | GET | `/api/ExchangeRateTask/GetStats` | 获取汇率填写统计 |
| 获取用户状态 | GET | `/api/ExchangeRateTask/GetUserStatus` | 获取用户汇率填写状态 |

### 管理接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取报表 | GET | `/api/ExchangeRateManage/GetReport` | 获取汇率填写统计报表 |
| 导出记录 | GET | `/api/ExchangeRateManage/ExportRecords` | 导出汇率填写记录 |
| 导入记录 | POST | `/api/ExchangeRateManage/ImportRecords` | 批量导入汇率数据 |

## 使用示例

### 1. 获取汇率任务配置

```javascript
// 前端调用
const response = await fetch('/api/ExchangeRateTask/GetConfig');
const result = await response.json();

if (result.code === 200) {
    console.log('配置信息:', result.data);
}
```

### 2. 保存汇率任务配置

```javascript
// 前端调用
const configData = {
    taskName: '每日汇率更新任务',
    executors: [1, 2, 3], // 用户ID数组
    notificationInterval: 5,
    status: 1,
    description: '每日提醒指定人员填写当天汇率'
};

const response = await fetch('/api/ExchangeRateTask/SaveConfig', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json'
    },
    body: JSON.stringify(configData)
});
```

### 3. 检查用户是否需要填写汇率

```javascript
// 前端调用
const response = await fetch('/api/ExchangeRateTask/CheckUserNeedFill');
const result = await response.json();

if (result.code === 200 && result.data.needFill) {
    // 显示汇率填写通知
    showExchangeRateNotification();
}
```

### 4. 提交汇率信息

```javascript
// 前端调用
const exchangeRateData = {
    currency: '1', // 币种ID
    exchangeRate: 7.2345,
    remark: '今日汇率',
    date: '2024-01-15'
};

const response = await fetch('/api/ExchangeRateTask/SubmitExchangeRate', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json'
    },
    body: JSON.stringify(exchangeRateData)
});
```

## 配置说明

### 1. 数据库配置

确保数据库连接字符串正确配置，并且数据库用户有足够的权限创建表和索引。

### 2. 认证配置

根据项目使用的认证方式（JWT、Session等），调整 `GetCurrentUserId()` 方法的实现。

### 3. 日志配置

建议配置日志记录，便于调试和监控：

```csharp
services.AddLogging(builder =>
{
    builder.AddConsole();
    builder.AddDebug();
});
```

### 4. 跨域配置

如果前端和后端分离部署，需要配置CORS：

```csharp
services.AddCors(options =>
{
    options.AddPolicy("AllowAll", builder =>
    {
        builder.AllowAnyOrigin()
               .AllowAnyMethod()
               .AllowAnyHeader();
    });
});
```

## 扩展功能

### 1. 添加权限控制

```csharp
[Authorize(Roles = "Admin")]
[HttpGet("GetConfig")]
public async Task<IActionResult> GetConfig()
{
    // 只有管理员可以获取配置
}
```

### 2. 添加缓存

```csharp
services.AddMemoryCache();

// 在服务中使用缓存
public async Task<ExchangeRateTaskConfig> GetTaskConfigAsync()
{
    var cacheKey = "ExchangeRateTaskConfig";
    if (_cache.TryGetValue(cacheKey, out ExchangeRateTaskConfig config))
    {
        return config;
    }
    
    // 从数据库获取并缓存
    config = await _db.Queryable<ExchangeRateTaskConfig>().FirstAsync();
    _cache.Set(cacheKey, config, TimeSpan.FromMinutes(30));
    
    return config;
}
```

### 3. 添加数据验证

```csharp
public class ExchangeRateSubmitRequestValidator : AbstractValidator<ExchangeRateSubmitRequest>
{
    public ExchangeRateSubmitRequestValidator()
    {
        RuleFor(x => x.Currency).NotEmpty().WithMessage("币种不能为空");
        RuleFor(x => x.ExchangeRate).GreaterThan(0).WithMessage("汇率必须大于0");
    }
}
```

## 注意事项

1. **数据库权限**：确保数据库用户有创建表、索引和存储过程的权限
2. **字符集**：建议使用 utf8mb4 字符集支持完整的 Unicode 字符
3. **时区**：注意处理时区问题，建议统一使用 UTC 时间
4. **性能优化**：对于大量数据，考虑添加分页和索引优化
5. **错误处理**：完善异常处理和日志记录
6. **安全考虑**：添加输入验证和权限控制

## 测试建议

1. **单元测试**：为服务层编写单元测试
2. **集成测试**：测试API接口的完整流程
3. **性能测试**：测试大量数据下的性能表现
4. **安全测试**：测试权限控制和输入验证

## 部署建议

1. **数据库迁移**：使用数据库迁移工具管理表结构变更
2. **配置管理**：使用配置文件管理不同环境的配置
3. **监控告警**：添加应用监控和错误告警
4. **备份策略**：制定数据库备份和恢复策略
