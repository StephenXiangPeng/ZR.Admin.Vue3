# 汇率填写功能问题修复说明

## 问题分析

根据您提供的错误信息，主要问题包括：

1. **JSON反序列化错误**：`executors` 字段无法转换为 `System.Int32[]` 类型
2. **缺少必需字段**：`request` 字段是必需的但缺失
3. **API路径错误**：前端使用了错误的API路径

## 修复方案

### 1. 前端数据格式修复

**问题**：前端发送的 `executors` 是字符串数组，后端期望整数数组

**修复前**：
```javascript
const requestData = {
    executors: exchangeRateTaskForm.executors.join(','), // 发送字符串
    status: exchangeRateTaskForm.status, // 可能是字符串
}
```

**修复后**：
```javascript
const requestData = {
    executors: exchangeRateTaskForm.executors.map(id => parseInt(id)), // 确保是整数数组
    status: parseInt(exchangeRateTaskForm.status), // 确保是整数
}
```

### 2. API路径修复

**问题**：前端使用了错误的API路径

**修复前**：
```javascript
const res = await request.post('ExchangeRateTask/SaveConfig/SaveConfig', requestData)
```

**修复后**：
```javascript
const res = await request.post('ExchangeRateTask/SaveConfig', requestData)
```

### 3. 数据加载兼容性修复

**问题**：后端返回的 `executors` 可能是字符串或数组格式

**修复**：
```javascript
// 处理executors数据，可能是字符串或数组
if (typeof res.data.executors === 'string') {
    exchangeRateTaskForm.executors = res.data.executors ? res.data.executors.split(',') : []
} else if (Array.isArray(res.data.executors)) {
    exchangeRateTaskForm.executors = res.data.executors.map(id => id.toString())
} else {
    exchangeRateTaskForm.executors = []
}
```

## 后端数据模型

确保后端的数据模型正确定义：

```csharp
public class ExchangeRateTaskConfigRequest
{
    [Required(ErrorMessage = "任务名称不能为空")]
    public string TaskName { get; set; }

    [Required(ErrorMessage = "执行人员不能为空")]
    public int[] Executors { get; set; } // 期望整数数组

    [Required(ErrorMessage = "通知间隔不能为空")]
    [Range(1, 60, ErrorMessage = "通知间隔必须在1-60分钟之间")]
    public int NotificationInterval { get; set; }

    [Required(ErrorMessage = "任务状态不能为空")]
    public int Status { get; set; } // 期望整数

    public string Description { get; set; }
}
```

## 测试步骤

### 1. 使用测试页面

我创建了一个测试页面 `汇率填写功能API测试.html`，您可以用它来测试API：

1. 在浏览器中打开测试页面
2. 点击"获取配置"测试获取功能
3. 填写表单并点击"保存配置"测试保存功能
4. 查看控制台和结果区域了解详细信息

### 2. 检查网络请求

在浏览器开发者工具中：

1. 打开 Network 标签页
2. 执行保存配置操作
3. 查看请求的 Payload 和 Response
4. 确认数据格式正确

### 3. 后端调试

在后端代码中添加日志：

```csharp
[HttpPost("SaveConfig")]
public async Task<IActionResult> SaveConfig([FromBody] ExchangeRateTaskConfigRequest request)
{
    // 添加调试日志
    Console.WriteLine($"接收到的数据: {JsonConvert.SerializeObject(request)}");
    
    try
    {
        if (!ModelState.IsValid)
        {
            Console.WriteLine($"模型验证失败: {JsonConvert.SerializeObject(ModelState)}");
            return BadRequest(ModelState);
        }
        
        // ... 其他代码
    }
    catch (Exception ex)
    {
        Console.WriteLine($"保存配置异常: {ex.Message}");
        return StatusCode(500, ex.Message);
    }
}
```

## 常见问题排查

### 1. 数据类型不匹配

**症状**：`The JSON value could not be converted to System.Int32[]`

**解决方案**：
- 确保前端发送的是整数数组，不是字符串数组
- 检查后端模型定义是否正确

### 2. 必需字段缺失

**症状**：`The request field is required`

**解决方案**：
- 检查前端是否发送了所有必需字段
- 确认后端模型验证规则

### 3. API路径错误

**症状**：404 Not Found

**解决方案**：
- 检查Controller路由配置
- 确认前端请求路径正确

## 验证修复

修复后，您应该能够：

1. ✅ 成功获取汇率任务配置
2. ✅ 成功保存汇率任务配置
3. ✅ 正确显示执行人员列表
4. ✅ 正常切换任务状态

## 下一步

1. 测试所有API接口
2. 验证前端功能完整性
3. 检查数据库数据是否正确保存
4. 测试用户权限和验证逻辑

如果还有问题，请提供：
- 浏览器控制台的错误信息
- 网络请求的详细信息
- 后端日志输出
