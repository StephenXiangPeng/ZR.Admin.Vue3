# 开发环境Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装必要的系统依赖
RUN apk add --no-cache git

# 复制package.json和yarn.lock
COPY package.json yarn.lock* ./

# 安装依赖
RUN yarn install

# 复制源代码
COPY . .

# 暴露开发服务器端口
EXPOSE 8887

# 启动开发服务器
CMD ["yarn", "dev"]
