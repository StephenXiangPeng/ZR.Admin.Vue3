# 汇率自动获取功能实现说明

## 功能概述

实现了系统中所有涉及汇率的单据在选择币种时自动获取当天最新汇率的功能。如果当天没有汇率，则自动使用前一天的汇率，确保业务单据能够使用最新的汇率信息。

## 实现方案

### 1. 后端API接口

#### 1.1 新增API接口
- **获取指定币种最新汇率**：`GET /api/ExchangeRateTask/GetLatestExchangeRate`
- **获取所有币种最新汇率**：`GET /api/ExchangeRateTask/GetAllLatestExchangeRates`

#### 1.2 汇率获取逻辑
```csharp
// 获取策略：当天 -> 前一天 -> 往前最多7天
public async Task<decimal?> GetLatestExchangeRateAsync(string currency, DateTime date)
{
    // 1. 查找指定日期的汇率
    // 2. 如果当天没有，查找前一天的汇率
    // 3. 如果前一天也没有，继续往前查找，最多查找7天
    // 4. 返回找到的最新汇率，找不到返回null
}
```

### 2. 前端通用服务

#### 2.1 汇率服务类 (`src/utils/exchangeRateService.js`)
```javascript
class ExchangeRateService {
  // 获取指定币种的最新汇率
  async getLatestExchangeRate(currency, date = null)
  
  // 获取所有币种的最新汇率
  async getAllLatestExchangeRates(date = null)
  
  // 根据币种ID获取币种名称
  getCurrencyName(currencyId, currencyOptions)
  
  // 获取默认汇率（用于人民币等特殊币种）
  getDefaultExchangeRate(currencyId)
  
  // 格式化汇率显示
  formatExchangeRate(rate, precision = 4)
}
```

#### 2.2 使用方式
```javascript
// 导入服务
import exchangeRateService from '@/utils/exchangeRateService'

// 在币种选择处理函数中使用
const handleCurrencyChange = async (value) => {
  if (value === '3') { // 人民币
    form.exchangeRate = '1.000'
  } else {
    // 获取最新汇率
    const latestRate = await exchangeRateService.getLatestExchangeRate(value)
    if (latestRate !== null) {
      form.exchangeRate = exchangeRateService.formatExchangeRate(latestRate)
    } else {
      // 使用默认汇率
      const defaultRate = exchangeRateService.getDefaultExchangeRate(value)
      form.exchangeRate = exchangeRateService.formatExchangeRate(defaultRate)
      ElMessage.warning('未找到最新汇率，已使用默认汇率')
    }
  }
}
```

### 3. 已更新的页面

#### 3.1 财务收款页面 (`src/views/finance/customercollection.vue`)
- 更新了 `handleForeignCurrencyChange` 函数
- 选择外销币种时自动获取最新汇率

#### 3.2 销售合同页面 (`src/views/sale/salecontract.vue`)
- 更新了 `foreignCurrencyChange` 函数
- 选择外销币种时自动获取最新汇率

#### 3.3 客户报价页面 (`src/views/sale/customerquotation.vue`)
- 更新了 `exportcurrencyChange` 函数
- 选择外销币种时自动获取最新汇率

## 功能特点

### 1. 智能汇率获取
- **优先级策略**：当天汇率 > 前一天汇率 > 往前最多7天
- **自动降级**：如果获取不到最新汇率，自动使用默认汇率
- **用户提示**：当使用默认汇率时，会提示用户

### 2. 容错处理
- **网络异常**：API调用失败时使用默认汇率
- **数据缺失**：找不到汇率记录时使用默认汇率
- **用户友好**：提供清晰的错误提示信息

### 3. 性能优化
- **单例服务**：汇率服务使用单例模式，避免重复创建
- **异步处理**：所有汇率获取都是异步的，不阻塞UI
- **缓存机制**：可以扩展添加缓存机制提高性能

## 默认汇率设置

```javascript
// 默认汇率配置
{
  '1': 7.200,  // 美元
  '2': 7.800,  // 欧元
  '3': 1.000   // 人民币
}
```

## 使用场景

### 1. 财务收款
- 选择外销币种时自动填充汇率
- 自动计算结汇金额

### 2. 销售合同
- 选择外销币种时自动填充汇率
- 影响利润计算和成本核算

### 3. 客户报价
- 选择外销币种时自动填充汇率
- 影响报价计算和成本分析

## 扩展性

### 1. 新增币种
- 在 `getDefaultExchangeRate` 方法中添加新币种的默认汇率
- 在 `GetAllLatestExchangeRatesAsync` 方法中添加新币种ID

### 2. 新增页面
- 导入 `exchangeRateService`
- 在币种选择处理函数中调用 `getLatestExchangeRate` 方法

### 3. 自定义逻辑
- 可以扩展汇率服务类，添加更多业务逻辑
- 可以添加汇率缓存、汇率历史查询等功能

## 测试建议

### 1. 功能测试
- 测试选择不同币种时的汇率自动填充
- 测试当天有汇率和无汇率的情况
- 测试网络异常时的降级处理

### 2. 性能测试
- 测试大量并发请求时的性能表现
- 测试汇率获取的响应时间

### 3. 用户体验测试
- 测试用户提示信息的准确性
- 测试界面响应速度

## 总结

通过实现汇率自动获取功能，系统现在能够：

1. **自动化**：用户选择币种时自动获取最新汇率
2. **智能化**：优先使用当天汇率，无则使用历史汇率
3. **容错性**：网络异常或数据缺失时有合理的降级处理
4. **一致性**：所有涉及汇率的单据都使用相同的逻辑
5. **可扩展**：易于添加新的币种和页面

这个功能大大提升了用户体验，减少了手动输入汇率的错误，确保了业务数据的准确性。
